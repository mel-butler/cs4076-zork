/*#ifndef NPC_H
#define NPC_H
#include "Character.h"

class NPC : public Character
{
public:
    NPC(string name, string description, bool status);
    void isDead();


private:
    bool status;
};

#endif // NPC_H
*/
